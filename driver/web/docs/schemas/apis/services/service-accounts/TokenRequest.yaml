required:
  - auth_type
type: object
properties:
  auth_type:
    type: string
    enum:
      - static_token
      - signature
  creds:
    anyOf:
      - $ref: "./CredsStaticToken.yaml"
      - $ref: "./CredsSignature.yaml"