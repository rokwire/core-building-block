required:
  - credential_id
  - reset_code
  - auth_type
type: object
properties:
  credential_id:
    type: string
  reset_code:
    type: string
  auth_type:
    type: string
    enum:
      - email
  params:
    type: object
    anyOf:
      - $ref: "../../../../../shared/requests/ParamsSetEmailCredential.yaml"