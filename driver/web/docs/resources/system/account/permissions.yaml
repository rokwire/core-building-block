put:
  tags:
  - System
  summary: Grant account permissions
  description: |
    Grant account permissions

    **Auth:** Requires system access token with `update_account-permissions` or `all_account-permissions` permission
  security:
    - bearerAuth: []
  requestBody:
    description: Permissions
    content:
      application/json:
        schema:
          $ref: "../../../schemas/apis/system/account/permissions/request/Request.yaml"
    required: true
  responses:
    200:
      description: Success
      content:
        text/plain:
          schema:
            type: string
            example: Success
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error