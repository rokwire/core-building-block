delete:
  tags:
  - Admin
  summary: Delete permissions from role
  description: |
    Delete permissions from role
  security:
    - bearerAuth: []
  requestBody:
    description: role ID for removing
    content:
      application/json:
        schema:
          $ref: "../../../../../schemas/apis/admin/application/roles/remove-permissions/request/Request.yaml"
  responses:
    200:
      description: Success
      content:
        text/plain:
          schema:
            type: string
            example: Success
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error  