delete:
  tags:
  - Admin
  summary: Delete permissions from account
  description: |
    Deletes an existing permissions from account
  security:
    - bearerAuth: []
  requestBody:
    description: Permissions
    content:
      application/json:
        schema:
          $ref: "../../../../../schemas/apis/admin/application/account/permissions/revoke/request/Request.yaml"
  responses:
    200:
      description: Success
      content:
        text/plain:
          schema:
            type: string
            example: Success
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error