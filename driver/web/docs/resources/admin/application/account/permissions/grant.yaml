put:
  tags:
  - Admin
  summary: Grant account permissions
  description: |
    Grant account permissions
  security:
    - bearerAuth: []
  requestBody:
    description: Permissions
    content:
      application/json:
        schema:
          $ref: "../../../../../schemas/apis/admin/grant-permissions/request/Request.yaml"
    required: true
  responses:
    200:
      description: Success
      content:
        text/plain:
          schema:
            type: string
            example: Success
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error