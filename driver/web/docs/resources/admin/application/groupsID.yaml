 delete:
  tags:
  - Admin
  summary: Remove application group
  description: |
    Remove application group

    **Auth:** Requires user auth token
  security:
    - bearerAuth: []
  parameters:
    - name: id
      in: path
      description: group ID
      required: true
      schema:
        type: string
  responses:
    200:
      description: Success
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error