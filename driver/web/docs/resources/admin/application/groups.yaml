 get:
  tags:
  - Admin
  summary: Get application groups
  description: |
    Gets application groups
  security:
    - bearerAuth: []
  responses:
      200:
        description: Successful operation
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: "../../../schemas/application/AppOrgGroupFields.yaml"
      400:
        description: Bad request
      401:
        description: Unauthorized
      500:
        description: Internal error
 post:
  tags:
  - Admin
  summary: Create application organization groups
  description: |
   Create application organization groups
  security:
    - bearerAuth: []
  requestBody:
    description: application organization groups
    content:
      application/json:
        schema:
          $ref: "../../../schemas/apis/admin/create-appOrgGroups/request/Request.yaml"
    required: true
  responses:
    200:
      description: Success
      content:
        text/plain:
          schema:
            type: string
            example: Success
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error