delete:
  tags:
  - Admin
  summary: Delete account from group
  description: |
    Deletes an existing account from group
  security:
    - bearerAuth: []
  requestBody:
    description: account ID for removing
    content:
      application/json:
        schema:
          $ref: "../../../../schemas/apis/admin/application/groups/remove-account/request/Request.yaml"
  responses:
    200:
      description: Success
      content:
        text/plain:
          schema:
            type: string
            example: Success
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error 