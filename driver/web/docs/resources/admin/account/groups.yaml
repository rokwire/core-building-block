put:
  tags:
  - Admin
  summary: Grant account groups
  description: |
    Grant account groups
  
    **Auth:** Requires auth token with `auth_admin` permission
  security:
    - bearerAuth: []
  requestBody:
    description: Groups
    content:
      application/json:
        schema:
          $ref: "../../../schemas/apis/shared/requests/account/groups/Request.yaml"
    required: true
  responses:
    200:
      description: Success
      content:
        text/plain:
          schema:
            type: string
            example: Success
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error