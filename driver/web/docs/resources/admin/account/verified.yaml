put:
  tags:
  - Admin
  summary: Update account verified flag
  description: |
    Update the verified flag associated with the user's account

    **Auth:** Requires user access token
  security:
    - bearerAuth: []
  requestBody:
    description: Verfied
    content:
      application/json:
        schema:
          $ref: "../../../schemas/user/Verified.yaml"
    required: true
  responses:
    200:
      description: Success
      content:
        text/plain:
          schema:
            type: string
            example: Success
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error