 get:
  tags:
  - Admin
  summary: Get organizations
  description: |
    Gets organizations
  responses:
      200:
        description: Successful operation
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: "../../schemas/apis/admin/get-organizations/response/Response.yaml"
      400:
        description: Bad request
      401:
        description: Unauthorized
      500:
        description: Internal error
 post:
  tags:
  - Admin
  summary: Create organization
  description: |
    Creates organization
  requestBody:
    description: creates one organization
    content:
      application/json:
        schema:
          $ref: "../../schemas/apis/admin/create-organization/request/Request.yaml"
    required: true
  responses:
    200:
      description: Success
      content:
        text/plain:
          schema:
            type: string
            example: Success
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error