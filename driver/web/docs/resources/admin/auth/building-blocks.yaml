get:
  tags:
  - Admin
  summary: Get the building blocks
  description: |
    Gets the building blocks
  security:
    - bearerAuth: []
  parameters:
  - name: app_id
    in: path
    description: app_id
    required: true
    style: simple
    explode: false
    schema:
      type: string
  responses:
    200:
      description: successful operation
      content:
        application/json:
            type: string
            items: 
              $ref: "../../../schemas/apis/shared/responses/building-blocks/Response.yaml"
    400:
      description: Bad request
    401:
      description: Unauthorized
    404:
      description: Not Found
    500:
      description: Internal error