 get:
  tags:
  - Admin
  summary: Get applications_organizations_groups
  description: |
    Gets applications_organizations_groups
  security:
    - bearerAuth: []
  responses:
      200:
        description: Successful operation
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: "../../../schemas/apis/system/get-appOrgGroups/response/Response.yaml"
      400:
        description: Bad request
      401:
        description: Unauthorized
      500:
        description: Internal error
 post:
  tags:
  - Admin
  summary: Create application_organization_groups
  description: |
   Create application_organization_groups
  security:
    - bearerAuth: []
  requestBody:
    description: application_organization_groups
    content:
      application/json:
        schema:
          $ref: "../../../schemas/apis/system/create-appOrgGroups/request/Request.yaml"
    required: true
  responses:
    200:
      description: Success
      content:
        text/plain:
          schema:
            type: string
            example: Success
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error