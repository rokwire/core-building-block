 get:
  tags:
  - Admin
  summary: Get applications_organizations_roles
  description: |
    Gets applications_organizations_roles
  security:
    - bearerAuth: []
  responses:
      200:
        description: Successful operation
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: "../../../schemas/apis/system/get-appOrgRoles/response/Response.yaml"
      400:
        description: Bad request
      401:
        description: Unauthorized
      500:
        description: Internal error
 post:
  tags:
  - Admin
  summary: Create application_organization_role
  description: |
    Create application_organization_role
  security:
    - bearerAuth: []
  requestBody:
    description: application_organization_role
    content:
      application/json:
        schema:
          $ref: "../../../schemas/apis/system/create-appOrgRoles/request/Request.yaml"
    required: true
  responses:
     200:
       description: Success
       content:
         text/plain:
           schema:
             type: string
             example: Success
     400:
       description: Bad request
     401:
       description: Unauthorized
     500:
       description: Internal error