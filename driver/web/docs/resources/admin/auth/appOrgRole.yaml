 get:
  tags:
  - Admin
  summary: Get applications_organizations_roles
  description: |
    Gets applications_organizations_roles
  security:
    - bearerAuth: []
  responses:
      200:
        description: Successful operation
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: "../../../schemas/apis/system/get-appOrgRoles/response/Response.yaml"
      400:
        description: Bad request
      401:
        description: Unauthorized
      500:
        description: Internal error
 post:
  tags:
  - Admin
  summary: Create applications_organizations_roles
  description: |
    Create applications_organizations_roles
  security:
    - bearerAuth: []
  requestBody:
    description: |
      Create applications_organizations_roles
  content:
    application/json:
      schema:
          $ref: "../../../schemas/apis/system/create-appOrgRoles/request/Request.yaml"
  responses:
      200:
        description: Successful operation
        content:
        text/plain:
            schema:
              type: array
              items:
                $ref: "../../../schemas/apis/system/create-appOrgRoles/response/Response.yaml"
      400:
        description: Bad request
      401:
        description: Unauthorized
      500:
        description: Internal error      
