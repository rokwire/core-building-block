post:
  tags:
  - Services
  summary: logout
  description: |
    logout an account
  security:
    - bearerAuth: []
  requestBody:
    description: all_session
    content:
      application/json:
        schema:
          $ref: "../../../schemas/apis/shared/requests/logout/Request.yaml"
  responses:
    200:
      description: Success
      content:
        text/plain:
          schema: 
              type: string
              example: Success
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error