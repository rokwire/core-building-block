post:
  tags:
  - Services
  summary: Get SSO login url
  description: |
    Retrieve a pre-formatted SSO login URL
  security:
    - bearerAuth: []
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../../../schemas/apis/services/login-url/request/Request.yaml"
    required: true
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: "../../../schemas/apis/services/login-url/response/Response.yaml"
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error