post:
  tags:
  - Services
  summary: Link credentials
  description: |
    Link credentials to an existing account
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../../../schemas/apis/services/link-creds/request/Request.yaml"
    required: true
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: "../../../schemas/apis/services/link-creds/response/Response.yaml"
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error